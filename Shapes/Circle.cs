using System;

namespace Shapes
{
    public class Circle
    {
        // Find area of a circle from radius
        public double Area(double r)
        {
            return Math.PI * r * r;
        }
    }
}
